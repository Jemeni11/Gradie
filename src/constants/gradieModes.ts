import type { GradieMode } from "@/types";

const gradieModes: GradieMode[] = [
  "Default",
  "Suprise Me!",
  "Bold Pop",
  "Soft Sweep",
  "Full Blend",
  "Custom",
];

export default gradieModes;
